#include<stdio.h>
int main()
{
  int n,m,i,min;
  scanf("%d",&n);
  scanf("%d",&min);
  i=1;
  for(i=1;i<n;i++)
  {
    scanf("%d",&m);
    if(min>m)
    {
      min=m;
    }
    else
    {
      min=min;
    }
  }
  printf("min = %d",min);
  return 0;
}
