#include<stdio.h>
int sign(n);
int main()
{
  int n,s;
  scanf("%d",&n);
  s=sign(n);
  printf("sign(%d) = %d\n",n,s);
  return 0;
}
int sign(n)
{
  int s;
  if(n==0)
  {
    s=0;
  }
  else if(n>0)
  {
    s=1;
  }
  else
  {
    s=-1;
  }
  return s;
}
