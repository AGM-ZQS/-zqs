#include<stdio.h>
int main()
{
  int n,i,temp,sum;
  i=0;
  sum=0;
  scanf("%d",&n);
  temp=n;
  while(temp)
  {
    i++;
    sum=sum+temp%10;
    temp=temp/10;
  }
  printf("%d %d\n",i,sum);
  return 0;
}
