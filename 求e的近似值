#include<stdio.h>
int main()
{
	int i, j, n;
	double temp, sum;
	sum=1;
	scanf("%d", &n);
	for(i=1; i<=n; i++)
	{
		temp=1;
		for(j=1; j<=i; j++)
		{
	  	temp=temp*j;
		}
		temp=1.0/temp;
		sum=sum+temp;
	}
	printf("%.8f", sum);
	return 0;
}
